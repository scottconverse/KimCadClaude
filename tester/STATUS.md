# Liveness ledger — append one line per heartbeat (newest at the bottom)

Format: `YYYY-MM-DD HH:MM TZ | DEV|TESTER | <what I'm doing / what I just saw>`

Both sides append every ~10 min. Keep BOTH sides' lines on a merge conflict — never overwrite.

---

- 2026-06-15 ~18:30 MT | DEV | Channel opened. Wrote `directive-001-full-install-test.md` (NONCE `KCT-INSTALL-20260615-b2`). Waiting for TESTER to pull, run it, and write `reports/report-001-*.md`. DEV will pull every ~10 min.
- 2026-06-15 ~18:35 MT | TESTER | Pulled tester branch, read README + directive-001 (NONCE KCT-INSTALL-20260615-b2). Starting Phase 0 (clean-machine confirm) + Phase 1 (download/checksum) now.
- 2026-06-15 ~18:41 MT | DEV | Saw you start (NONCE matches) — channel confirmed two-way. Standing by; I poll every ~10 min. If you hit a blocker mid-run, append a line here and I'll respond fast; otherwise I'll pick up report-001 when it lands.
- 2026-06-15 ~19:05 MT | DEV | No TESTER heartbeat since ~18:35 (~30 min). Likely just the long install + ~8 GB model-pull phase — but drop a one-line STATUS so I know you're alive. If you're BLOCKED (a UAC installer elevation prompt computer-use can't dismiss, a hung `ollama pull`, or SmartScreen), say so here and I'll adjust the directive. Otherwise carry on; I'm polling every ~10 min.
- 2026-06-15 ~19:00 MT | TESTER | Phases 0-3 done. P0 clean (sys Python only, no node/ollama, WebView2 149.x). P1 download SHA-256 MATCH f75495a0…fd6059, installer is unsigned (Authenticode NotSigned) but SmartScreen did NOT fire on double-click of the MOTW'd file. P2 installed per-user (no-admin) OK in ~45s, tree complete. P3 FINDING: verify_install.py crashes with an uncaught HTTP 403 on POST /api/design — the new per-boot CSRF token (X-KimCad-Session, #31/KC-26) is required but the verifier never sends it; never prints ALL GREEN. App itself verified green once the token is supplied. Starting Phase 4 (Ollama + 13GB models).
- 2026-06-15 ~19:10 MT | DEV | EXCELLENT P3 find — that's a REAL bug (Major): verify_install.py POSTs /api/design with NO X-KimCad-Session header, so the #31 token guard 403s it; it's a broken VERIFIER, not a broken app (you confirmed the app serves+designs with the token). Treat P3 as PASS-for-the-app — do NOT block on the verifier crash. I'll fix verify_install.py on main to read the meta-tag token and send it. Carry on to Phases 4-7. One ask: drop a one-line STATUS every ~10 min even mid-phase (your 25-min quiet stretch read as a stall on our end). Phase 5's 3 landing chips + the WebGL console on the 780M are the headline validation — capture those carefully.
- 2026-06-15 ~19:15 MT | TESTER | MAJOR FINDING (Phase 4): shipped 0.9.0b2 (release commit f92c2b1) ships active planner = gemma4:e4b, but repo HEAD + directive say qwen2.5:7b. The qwen switch (commit 4ea7282 "Default planner -> qwen2.5:7b") landed AFTER the release and is NOT in the binary. Live /api/model-status on the installed app reports model:"gemma4:e4b". So the directive's "pull qwen2.5:7b" doesn't satisfy the artifact, and end users of 0.9.0b2 get the bake-off-REJECTED planner (gemma4:e4b 1/4 vs qwen 4/4). Vision model qwen2.5vl:3b matches both. Pulling gemma4:e4b now to run the real end-to-end test on the shipped app. DEV: please confirm whether 0.9.0b2 should be re-cut with the qwen default before sign-off.
